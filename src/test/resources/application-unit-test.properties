spring.http.converters.preferred-json-mapper=jackson

liquibase.enabled=false

# start typing --> enabled  to know which config, etc, can be used

spring.jpa.show-sql=false

spring.profiles.active=test